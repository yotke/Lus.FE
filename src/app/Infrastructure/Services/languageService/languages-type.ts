// Language codes supported by the app (same enum pattern as ArmyLuz/Weekeye).
// To add a language: add it here, mark active in LANGUAGE_CONFIG, and ship
// assets/i18n/<code>.json.
export enum LanguagesType {
  En = 'en',
  He = 'he',
  Ru = 'ru',
  Ar = 'ar',
  Fr = 'fr',
  Es = 'es',
}
